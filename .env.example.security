# ============================================================================
# SECURITY CONFIGURATION (REQUIRED FOR PRODUCTION)
# ============================================================================

# CORS Security - CRITICAL: Restrict allowed origins for API access
# NEVER use "*" in production with credentials enabled
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
# For production, use your actual domain:
# ALLOWED_ORIGINS=https://app.yourdomain.com,https://api.yourdomain.com

# Trusted Hosts - CRITICAL: Restrict allowed hosts
# Comma-separated list of allowed hostnames
ALLOWED_HOSTS=localhost,127.0.0.1
# For production, use your actual domains:
# ALLOWED_HOSTS=app.yourdomain.com,api.yourdomain.com

# JWT Secret Key - REQUIRED: Change this for production!
JWT_SECRET_KEY=your-secret-key-change-in-production
# Generate a secure random key:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

# Authentication Backend - RECOMMENDED: Use Redis for production
USE_REDIS_AUTH=false
# Set to true for production deployments

# Redis Configuration (required if USE_REDIS_AUTH=true)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_AUTH_DB=1
TOKEN_TTL_SECONDS=3600

# Alternative: SQLite Database (for single-server deployments)
# AUTH_DB_PATH=githound_auth.db

# ============================================================================
# PRODUCTION SECURITY CHECKLIST
# ============================================================================
# [ ] Set unique JWT_SECRET_KEY
# [ ] Configure ALLOWED_ORIGINS with specific domains (no wildcards)
# [ ] Configure ALLOWED_HOSTS with specific domains (no wildcards)
# [ ] Enable persistent session storage (USE_REDIS_AUTH=true or SQLite)
# [ ] Set up Redis server for production
# [ ] Configure rate limiting (REDIS_HOST, REDIS_PORT)
# [ ] Enable HTTPS/TLS for all connections
# [ ] Review and test CORS policy
# [ ] Set up monitoring and alerting
# [ ] Regular security audits

# ============================================================================
# SECURITY REFERENCES
# ============================================================================
# - CORS Security: https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS
# - JWT Best Practices: https://datatracker.ietf.org/doc/html/rfc8725
# - Redis Security: https://redis.io/docs/management/security/
